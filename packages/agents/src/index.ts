// @tacit/agents — Agent SDK harnesses: archaeologist, judge escalation, interviewer (P1). Every harness takes maxTurns + cost ceiling from config/agent-budgets.ts. Week 4
// Empty-but-compiling workspace (Session 1). Real code lands in the session noted above.
export {};

// @tacit/gateway — Model gateway: per-stage routing (routing.yaml), fallbacks, prompt caching, cost/latency logging (F-CMP-3, F-CMP-5). The only package allowed to import LLM SDKs. Session 4
// Empty-but-compiling workspace (Session 1). Real code lands in the session noted above.
export {};

// @tacit/mcp-server — MCP server: tacit_lookup / tacit_get_artifact / tacit_get_sources, per-user auth, ACL filter at query time (F-SRV-1..4). Week 5
// Empty-but-compiling workspace (Session 1). Real code lands in the session noted above.
export {};

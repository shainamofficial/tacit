// @tacit/interviews — Channel-agnostic interview objects + Slack/email adapters; global per-person rate limit lives in limits.ts (F-INT-4). P1
// Empty-but-compiling workspace (Session 1). Real code lands in the session noted above.
export {};

// @tacit/pipeline — pg-boss compile stages: filter -> extract -> draft -> judge -> contradict; idempotent, resumable, budget-capped (F-CMP-2). Sessions 7-10
// Empty-but-compiling workspace (Session 1). Real code lands in the session noted above.
export {};

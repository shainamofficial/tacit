// @tacit/schema — Artifact JSON Schemas, zod types, and SQL migrations. PROPOSE-ONLY: changes go through a PR (CLAUDE.md ownership). Migrations: ./migrations
// Empty-but-compiling workspace (Session 1). Real code lands in the session noted above.
export {};
